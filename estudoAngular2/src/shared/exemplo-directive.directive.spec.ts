/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ExemploDirectiveDirective } from './exemplo-directive.directive';

describe('ExemploDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ExemploDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
